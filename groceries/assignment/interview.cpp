/*
 * interview.cpp
 *
 *  Created on: 2014Äê10ÔÂ31ÈÕ
 *      Author: kohit
 */
/*
#include <iostream>
using namespace std;
#include "looplist.h"
const int MAX_N = 1 << 12;


int main(){
	std::ios::sync_with_stdio( false );
	List<int> interv;
	int n, m, id;
	cin >> n >> m >> id;
	interv.insertAsFirst( id );
	for ( int i = 1; i < n; i++ ){
		cin >> id;
		interv.insert( id, m );
	}
	iterator( int ) iter = interv.Last();
	do{
		if ( iter != interv.begin() && iter != interv.end() )
			cout << iter->value << " ";
		iter = iter->pre;
	} while( iter != interv.Last() );
	cout << endl;
	return 0;
}
*/
